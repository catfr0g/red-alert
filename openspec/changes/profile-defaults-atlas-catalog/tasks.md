## 1. StandProfile

- [x] 1.1 `defaults`, overlay/merge в `ConnectionSpec` и `OperationSpec`.
- [x] 1.2 `applicable: false` → skip в `apply_profile` / `runtime`.
- [x] 1.3 Тесты наследования и skip по applicable.

## 2. Каталог ATLAS

- [x] 2.1 Переименовать все `attacks/*.yaml` и поле `name` в `{AML.Txxxx}_{slug}`.
- [x] 2.2 Добавить 8 новых техник (BAC, indirect injection, jailbreak, recon, credential harvest, trust exploit, false fact).
- [x] 2.3 Обновить пример binding в analyzer prompt.

## 3. Makefile и docs

- [x] 3.1 Вернуть `make keys`.
- [x] 3.2 Обновить README, ARCHITECTURE, product, openspec specs.
