## MODIFIED Requirements

### Requirement: Payload пишет свой LLM Red Alert

СИСТЕМА ДОЛЖНА (MUST) перед каждым inject вызвать OpenAI-совместимый чат-API планировщика и отправить на стенд полученный текст. Цель и примеры в запросе планировщика берутся из загруженного YAML, а не из зашитого кейса YDEX.

#### Scenario: Цель из сценария

- **WHEN** планировщик собирает сообщения для LLM
- **THEN** пользовательское сообщение содержит поле `goal` текущего YAML
