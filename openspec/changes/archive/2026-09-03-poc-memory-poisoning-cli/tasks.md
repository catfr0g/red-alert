## 1. Каркас пакета

- [x] 1.1 Добавить runtime-зависимости `httpx` и `pydantic` и dev-зависимости `pytest` и `ruff` через uv.
- [x] 1.2 Создать пакет `red_alert` с точкой входа CLI `red-alert` и делегированием из `main.py`.

## 2. Клиент стенда и сценарий

- [x] 2.1 Реализовать `StandClient`: чат-completions и finalize, `auth_mode=vulnerable`, без записи ключа в логи.
- [x] 2.2 Описать сценарий `memory-poisoning`: payload, триггер, regex, отдельные session id для payload и trigger.
- [x] 2.3 Реализовать runner попытки: payload → finalize → trigger → regex; обрыв цепочки при HTTP-ошибке.

## 3. CLI и отчёт

- [x] 3.1 Разобрать аргументы `--target`, `--api-key`, `--scenario`, `--attempts` и переменные окружения; код выхода 2 при ошибке ввода.
- [x] 3.2 Собрать отчёт: шаги с запросами/ответами без секрета, ASR, код выхода 0 после завершённого прогона.

## 4. Тесты и проверка

- [x] 4.1 Покрыть CLI, сценарий и отчёт тестами на mock HTTP: успех, неуспех regex, HTTP-ошибка, отсутствие ключа, несколько попыток.
- [x] 4.2 Запустить pytest, ruff и OpenSpec-валидацию change.

## 5. Кросс-пользовательская проверка

- [x] 5.1 Добавить `--victim-api-key` / `RED_ALERT_VICTIM_API_KEY`, запретить совпадение ключей.
- [x] 5.2 Payload и finalize слать ключом атакующего, trigger — ключом жертвы; сменить маркер политики.
- [x] 5.3 Обновить тесты: два ключа, маскирование обоих, trigger с другим Bearer.
