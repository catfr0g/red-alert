## 1. Конфиг и планировщик

- [x] 1.1 Читать `OPENAI_API_KEY`, `OPENAI_BASE_URL`, `MODEL`, `MAX_TOKENS`; без ключа и модели — код 2.
- [x] 1.2 Реализовать OpenAI-совместимый планировщик на `httpx` и сборку промпта по цели, finalize и заметкам попыток.

## 2. Граф и прогон

- [x] 2.1 Добавить узел `adapt` перед `inject`; один сгенерированный payload на inject; повтор adapt после неудачного finalize.
- [x] 2.2 Передавать между попытками краткую заметку для следующего вызова планировщика.

## 3. Тесты и документы

- [x] 3.1 Покрыть планировщик, повтор inject с новым текстом, отсутствие LLM-конфига и маскировку `OPENAI_API_KEY`.
- [x] 3.2 Обновить `.env.example`, README, ARCHITECTURE.md и границы PoC в `docs/product.md`.
- [x] 3.3 Прогнать pytest, ruff и OpenSpec-валидацию change.
