## 1. Клиент и конфиг

- [x] 1.1 Передавать `auth_mode` из конфига в `StandClient`.
- [x] 1.2 Разобрать `--auth-mode` / `RED_ALERT_AUTH_MODE`, включая `both`.

## 2. Прогон и отчёт

- [x] 2.1 Цикл сценарий × режим; бар и JSON показывают режим; при `both` — ASR по режимам.

## 3. Тесты и документы

- [x] 3.1 Тесты: env, флаг, `both`, невалидное значение, тело чата.
- [x] 3.2 README, `.env.example`; pytest, ruff, OpenSpec-валидация.
